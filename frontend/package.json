{
  "name": "@agent-office/frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canvas 2D pixel office frontend (milestone M3)",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp -r src dist/ && cp -r assets dist/"
  }
}